# DGX-SPARK-COMFYUI-DOCKER

This repository contains Ansible playbooks and Dockerfiles to set up ComfyUI on an NVIDIA DGX Spark cluster.

## Trusting SSH Hosts

```Powershell
wsl ./trust_ssh_hosts.sh --user du spark-3712.local
.\trust_ssh_hosts.bat --user du spark-3712.local
```

